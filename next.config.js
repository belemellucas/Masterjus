/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'www.masterjus.com.br', 'images.pexels.com', 'www.wordstream.com']
    }
}

module.exports = nextConfig
