import React from 'react';

const BlogPost = ({ blogs }) => {
  console.log(blogs)
   return (
    <div></div>
   )
}
  // <div className="flex flex-col text-sm font-medium leading-4 text-right text-neutral-600 max-md:mt-10">
  //   <img loading="lazy" src={imageSrc} alt={title} className="w-full aspect-square" />
  //   <div className="mt-7">{title}</div>
  //   {content.map((line, index) => (
  //     <div key={index} className={`mt-${index === 0 ? '2.5' : '3'}`}>{line}</div>
  //   ))}
  // </div>
    


export default BlogPost;